@model IEnumerable<Alpaca.Web.CoreAlertTool.Models.AlertType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AlertTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AlertTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AlertTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AlertTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AlertTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
